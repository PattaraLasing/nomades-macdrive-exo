import { TotalItemPipe } from './total-item-pipe';

describe('TotalItemPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalItemPipe();
    expect(pipe).toBeTruthy();
  });
});
